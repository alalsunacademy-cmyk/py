# مكتبة واجهات المستخدم
flet>=0.9.0

# ملاحظة: باقي المكتبات (sqlite3, hashlib, datetime, threading, time, re)
# هي مكتبات مدمجة مع Python ولا تحتاج إلى تثبيت     # للتعامل مع قاعدة البيانات
hashlib          # لتشفير كلمات المرور
datetime         # للتعامل مع التواريخ والأوقات
threading        # للتحديث التلقائي للمحادثات
time             # للتعامل مع الوقت والتأخيرات
re               # للتعابير النمطية (في utils.py)
